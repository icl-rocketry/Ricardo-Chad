#include "potHandler.h" 

void potHandler::extendedCommandHandler_impl(const NRCPacket::NRC_COMMAND_ID commandID, packetptr_t packetptr) {

    if (static_cast<int>(commandID) == 8) {
        
    }
}
